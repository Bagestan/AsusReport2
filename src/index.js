import { app, port } from "./server.js";

app.listen(port, () => {
  console.log(`Open http://localhost:${port}`);
  console.log(`Open http://localhost:${port}`);
  console.log(`Open http://localhost:${port}`);
  console.log(`Open http://localhost:${port}`);
  console.log(`Open http://localhost:${port}`);
  console.log(`Open http://localhost:${port}`);
});
